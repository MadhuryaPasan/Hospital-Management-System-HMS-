/**
 * Available Rols => Admin,Doctor,Nurse,Receptionist,Patient
 */

export enum Role {
  Admin = 'admin',
  Doctor = 'doctor',
  Nurse = 'nurse',
  Receptionist = 'receptionist',
  Patient = 'patient',
}

// defin a fixed, restricted set of named constants under a single custom data type.
